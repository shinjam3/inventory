import { StyleSheet } from "react-native";

export const homePageStyles = StyleSheet.create({
  noUnitsText: {
    fontSize: 20,
    textAlign: "center",
  },
  flatListContainer: {
    padding: 20,
    flex: 1,
  },
  addButton: {
    marginBottom: 40,
  }
});